# Legacy XRPL Demo Client

This directory preserves the original XRPL Hackathon MVP demo client from `main`.

It is kept for local reference only. The production build and VPS deployment use the newer `client/` app and do not include `client-legacy/`.
